{
  "name": "Ged-Deployment",
  "version": "0.1.0",
  "dependencies": {
    "grunt": "~0.4.5",
    "grunt-cli": "^0.1.13",
    "grunt-contrib-compress": "^0.14.0",
    "grunt-exec": "^0.4.6",
    "grunt-markdown": "^0.7.0"
  },
  "scripts": {
    "init": "grunt init",
    "build": "grunt build",
    "deploy": "grunt deploy"
  }
}
